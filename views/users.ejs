<!-- /views/users.ejs -->
<%- include('navBar') %>

<h2>Kullanıcılar</h2>

<ul>
    <% users.forEach(user => { %>
        <li><%= user.username %> - <%= user.email %></li>
    <% }); %>
</ul>
